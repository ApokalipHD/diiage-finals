Ce dossier contiendra les templates Helm (Deployments, Services, Ingress, etc.).

Exemple :

- `namespace.yaml` pour créer le namespace cible
- `app.yaml` pour déployer une application de démo

Pour l’instant, ce repo fournit uniquement la structure.

